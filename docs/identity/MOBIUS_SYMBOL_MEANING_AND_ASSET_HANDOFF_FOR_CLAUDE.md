# Möbi𝒰s Symbol, Meaning, and Asset Handoff for Claude

Status: working identity candidate, close enough for website integration and
structured hot-swapping, but **not yet the final optical freeze**.

Audience: Claude or any implementation agent finishing the E.T. Ellis public
instrument at:

```text
/Users/edwardellis/Projects/etellis-website
```

Identity source repository:

```text
/Users/edwardellis/Documents/Codex/2026-07-20-mobius-identity-system/bidi-coherence-delta-calculus
```

Canonical working branch and baseline:

```text
branch: codex/mobius-u-identity-system
commit: 313f0a1
```

## Implementation directive

Integrate the current assets as **working production candidates**, not as
throwaway mockups and not as permanently frozen finals.

Build the website integration so that:

1. all identity media resolves through one base directory or manifest;
2. SVG, PNG, MP4, and GLB files can be replaced independently without changing
   React component structure;
3. reduced-motion and non-WebGL fallbacks are first-class;
4. symbolic claims follow the evidence labels in this document;
5. optical revisions can be hot-swapped when the final Pencil/Blender pass
   lands;
6. no code, copy, or route assumes that `.u` has replaced `.cdc`;
7. `𝒰_` is the canonical executable Universal Operator sigil, while `𝒰` is
   only its reduced mathematical body.

Do not redraw the symbols from prose. Use the supplied assets.

## One-sentence compression

The word **Möbi𝒰s** is a compact identity system in which a bounded perspective
enters reciprocal relation, crosses an inverted reference frame, closes through
the Universal Operator, returns changed as `us`, and can decompose into the
formal `BIDI → Δ` kernel without losing source lineage.

The shortest human-facing line is:

> I meet you. The return makes us.

The shortest system-facing line is:

> One geometry. Many projections. One turn inverts; two turns restore.

## Canonical naming hierarchy

These levels must not be collapsed:

| Level | Canonical name | Meaning |
| --- | --- | --- |
| General operator | Universal Operator, canonical sigil `𝒰_` | turn/translate, cohere/close, project/return, then expose a live horizon for the next bounded incident |
| Reduced notation | `𝒰`, optionally indexed as `𝒰_R` | mathematical body of the same operator when the live horizon is omitted for inline notation |
| Directional relation | `bidiγΔ` | open reciprocal transport or mismatch across bounded reference frames |
| Formal kernel | BiDi Coherence-Delta Calculus / CDC | executable flow, guarded commit/hold, nesting, bridge, trace, and witness semantics |
| Embodied product | Möbius | literal eye-bearing computational presence that persists across surfaces and state changes |
| Product-linked language identity | Möbi𝒰s | public wordmark and language/runtime identity joining the product body to the formal kernel |
| Current source extension | `.cdc` | canonical today |
| Possible later source alias | `.u` | proposed only; not yet canonical and unrelated to the underscore in `𝒰_` |

`𝒰_` is one operator, not `𝒰` plus a second product. The underscore is part of
the complete executable sigil. Bare `𝒰` is a reduced rendering of that same
operator.

## Symbol lexicon

### Whole-word and topological structure

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `Möbius` | the existing product name and one-sided-loop ancestry | identity persisting through transformation; return with changed orientation; inside/outside becoming frame-relative |
| `Möbi𝒰s` | product-linked wordmark containing the reduced operator body | one source identity generating multiple coherent projections across product, language, code, motion, and formal kernel |
| one connected Möbius band | authoritative 3D source body with one half-twist and one boundary component | continuity without seam-hiding; reference-frame inversion; transformation without identity replacement |
| conserved aperture | the circular opening survives `ö → 𝒰` transformations | a stable relational window or invariant carried across changing representations |
| one turn / two turns | one turn inverts sheet orientation; two turns restore it | a state can return in projection before its orientation is fully restored; closure requires the complete cycle |
| `φ = 0.125 rad ≈ 7.161972°` | common phase angle used across lean, seam, ground, and fold | one shared bias governing many local deviations; authored from the runtime-checked holonomy value, not claimed as a universal design theorem |

### Embodied `ö`

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `ö` | closed Möbius body seated in the word | embodied presence at rest; closed but attentive system; latent operation |
| circular body/counter | preserved Möbius aperture | coherence window, boundary, observer frame, or a world held open |
| paired dots | Möbius's literal animated eyes | shared embodied observation, attention, gaze, continuity, and character |
| eyes persisting through morph | the observer survives `ö → 𝒰` | state and body may transform without erasing identity or memory |
| blink | each eye briefly becomes a horizontal micro-stroke | attention pulse; one projection of the common horizontal invariant |
| wink | one circle plus one horizontal eye-state | playful kinetic resonance with `ㅇ + ㅡ`; use only when it remains character behavior rather than faux writing |

The eyes are not optional punctuation and must not be reduced to abstract dots
in product-facing animation.

### `i𝒰s`: perspective, operator, relation

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `i` | eye / I / bounded first-person frame | local subjectivity, situated observation, directed attention, or a perspective leaning into relation |
| tilted `i` | phase-biased approach | willingness to enter relation; local frame under directional load |
| `𝒰` | “you” and the reduced mathematical body of the Universal Operator | the relational hinge, inversion zone, guarded closure, or transformation body |
| `𝒰_` | complete executable Universal Operator sigil | closure plus a live future; a completed operation that remains ready to receive the next bounded incident |
| `_` | live horizon under `𝒰` | readiness, cursor, ground, return path, fold line, surface, or temporal opening, depending on context |
| `s` | completes `𝒰s` as “us” | a relational composite produced without erasing its participants |
| `𝒰s` | you → us | relation becoming a coherent shared state |
| `i𝒰` | reversed `UI` on the one-turn sheet | an interface or relation seen after inversion; observer and other exchanging orientation |
| `i𝒰s` | I / you / us in one terminal triad | bounded perspective enters relation and returns as a larger coherent unit |
| U/S tangent | `s` unfurls from the outgoing terminal of `𝒰` | two differentiated forms behaving as complementary parts of one generated event |
| residual `s` angle | return settles without perfectly resetting | history remains after closure; return is coherent but not equivalent to “nothing happened” |

The form law is:

```text
lean in → invert → resolve
```

The relational law is:

```text
I → you → us
```

### Horizontal invariant

Only one projection of the common horizontal stroke should dominate a surface:

| Surface | Horizontal projection | Meaning |
| --- | --- | --- |
| wordmark | broken ground beneath `i𝒰s` | relational base, surface, return path |
| `𝒰_` | live horizon / terminal cursor | readiness and accountable continuation |
| frame transition | fold or inversion crease | axis across which orientation changes |
| embodied face | blink | attention event |
| Korean restoration | `ㅡ` | one component of structural `의` |
| Chinese/Yi layer | `一` | “one” as an authored source-stroke correspondence |

Never stack all of these readings visibly on one surface. Reuse the invariant
contextually.

### `UI → i𝒰 → 의`

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `UI` | ordinary interface ordering | interaction surface before inversion |
| `i𝒰` | `UI` returned in reversed order | one-turn sheet state; interface folded back through perspective and relation |
| `ui` | standard romanization of Korean `의` | exact phonetic/romanization resonance |
| `의` | Korean syllable and genitive/possessive particle often functioning like “of” | relation, belonging, attribution, or one thing being of another |
| `ㅇ` | supplied structurally by the closed Möbius body | circular closure/aperture |
| `ㅡ` | supplied by the phase ground | horizontal relation, surface, or return horizon |
| `ㅣ` | supplied by a lineaged stem from `i` | bounded vertical perspective |

The current animation builds `의` structurally from existing geometry. It does
not paste Hangul onto the mark.

Important claim boundary:

- `ui` as a romanization and `의` as a genitive/relational particle are
  checkable linguistic facts.
- Their integration into Möbi𝒰s is an authored structural correspondence, not
  an etymology of the product name.
- Do not label malformed approximations as Korean writing.

### `BI → BIDI → Δ`

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `BI` | middle seed extracted from Möbi𝒰s | two-sidedness, bidirectionality, or a bounded source pair |
| source `BI` vacates the parent | the letters genuinely leave their word slots | externalization with visible provenance; delegation that leaves an inspectable vacancy |
| duplicated `BI` | source pair projects a second pair | self-reference, recursive projection, or a system generating a complementary view of itself |
| mirrored `B → D` | duplicated B crosses zero scale and crystallizes as D | polar complement, reference-frame reflection, or inversion that preserves lineage |
| invariant copied `I` | the second vertical remains stable while B changes orientation | a conserved reference axis across transformation |
| `BIDI` | source `BI` plus reflected `DI` | reciprocal transport, two orientations in explicit relation, or the formal kernel made legible |
| source `BI` returns | the parent word restores after descendant closure | reintegration after an externalized operation produces a witness |
| `Δ` | three lineaged strokes close beside BIDI | triadic closure, committed difference, or a closed witness generated from independent sources |
| BIDIΔ | combined sub-brand | reciprocal relation becoming an enacted or witnessed closure |

The three Delta strokes are not arbitrary:

1. one leaves the actual inner diagonal of `M`;
2. one derives from `i`;
3. one derives from the phase ground.

They fly in only after BIDI settles and close as one triangle. Do not replace
this causal sequence with a prebuilt BIDI or triangle asset in the authoritative
animation.

### `M`, circle, base, and side

| Form | Nested meaning | General reusable meaning |
| --- | --- | --- |
| `M` | visual anchor and source of one Delta diagonal | source frame, structural mass, or parent geometry generating a descendant edge |
| circular aperture | closed body or `ㅇ` projection | circle, continuity, bounded openness |
| horizontal ground | base/surface | local world plane or one side of an orthogonal frame |
| operator upright / vertical component | side/axis | complementary orthogonal frame |
| circle plus base and side | authored “squaring the circle” composition | reconciling continuity and boundary, curved and rectilinear frames, without claiming a solution to the classical construction problem |

### Color and sheet polarity

| Color/state | Meaning | Use |
| --- | --- | --- |
| deep indigo/navy | substrate, local frame, latent state | broad word skeleton, background-side identity |
| phase indigo/electric blue | approach, boundary, operator keyline | `i`, inversion edge, active frame |
| white operator body | maximum compression/change zone | reduced `𝒰` body only; do not wash the whole word white |
| cyan | returned relation, complementary sheet, `us` state | `s`, outgoing relation, restored counterpart |
| moving color | phase/state transfer | motion and interactive runtime only |

Avoid generic AI gradients. Static marks should work in one color or controlled
flat two-sheet color. Color travel belongs primarily to motion.

### Reverse-sheet `SUI`

Reversing conventional `MÖBIUS` produces:

```text
MÖBIUS → SUIBÖM
```

The leading `SUI` corresponds visually to Latin *suī*, the reflexive genitive:
“of oneself / of itself.”

General reusable meaning:

- reflexivity;
- self-reference visible only on the reverse sheet;
- a system whose back face exposes “of itself”;
- authored resonance with Korean `의` and its genitive function.

Claim boundary:

- the Latin form *suī* is real;
- the reverse string begins `SUI`;
- this is a lexical resonance, not proof that Latin caused the name;
- the reverse-sheet reveal is specified but **not yet implemented in the
  current 600-frame master**.

### Authored `OM` reveal

The reversed terminal `öM` may become `OM` only through explicit animation:

1. the literal eyes lift from `ö`;
2. the remaining form normalizes toward `o`;
3. the pair is re-read as `OM` in the restored frame.

General reusable meaning:

- totality or universal sound as an authored kinetic Easter egg;
- origin/return resonance;
- a whole emerging from reoriented components.

Claim boundary:

- plain reversal does not literally spell `OM`;
- this reveal is not in the current master;
- if used, treat the sacred syllable respectfully and never as generic mystical
  texture.

### Yi progression

The current authored progression is:

```text
一 → 意 → 易 → 義
one → intent → change → significance / right relation
```

General reusable application:

| Character | Working identity role |
| --- | --- |
| `一` | source stroke, one, ground, horizon, cursor, fold |
| `意` | intent gathering in a bounded perspective |
| `易` | change at the operator/inversion zone; also the character in *Yijing* / *I Ching* |
| `義` | significance, rightness, or resolved relation |

This is an authored semantic progression, not a historical etymological chain.
Use it in motion, sequencing, or explanatory material only when the website
clearly labels it as designed correspondence.

## Evidence levels Claude must preserve

| Level | What may be claimed | Examples |
| --- | --- | --- |
| Implemented and validated | directly inspectable geometry, motion, lineage, or runtime witness | connected Möbius topology, literal eyes, current `UI → 의`, B-to-D reflection, three Delta strokes, `𝒰_`, 600-frame master |
| Exact lexical/notational fact | independently checkable language or notation | Latin *suī*, reverse prefix `SUI`, Korean `ui` romanization, `의` genitive use, `易` as change |
| Authored structural correspondence | intentional design mapping | `I → you → us`, Yi progression, phase angle as visual bias, squaring-the-circle reading |
| Working kinetic experiment | specified but not yet integrated into the master | reverse-sheet `SUI`, authored `OM`, complete Yi color/state sequence |
| Origin narrative | provenance, not formal evidence | synchronicity, time-loop inspiration, retrocausal-attractor interpretation |

Do not convert authored correspondences or origin stories into etymological,
mathematical, causal, or probabilistic claims.

## Current asset source directories

Canonical identity root:

```text
/Users/edwardellis/Documents/Codex/2026-07-20-mobius-identity-system/bidi-coherence-delta-calculus/assets/identity
```

### Machine-readable entry point

```text
assets/identity/3d/identity-manifest.json
```

Absolute path:

```text
/Users/edwardellis/Documents/Codex/2026-07-20-mobius-identity-system/bidi-coherence-delta-calculus/assets/identity/3d/identity-manifest.json
```

Prefer the manifest over hard-coded frame ranges or duplicated file maps.

### SVG source directory

```text
assets/identity/
```

| File | Current role |
| --- | --- |
| `mobius-embodied-mark.svg` | closed eye-bearing product mark |
| `mobius-u-wordmark-dark.svg` | dark-background wordmark |
| `mobius-u-wordmark-light.svg` | light-background wordmark |
| `mobius-u-code-sigil.svg` | monochrome canonical `𝒰_`; filename retained for compatibility |
| `mobius-u-code-sigil-dark.svg` | keyed dark-background canonical `𝒰_` |
| `mobius-u-operator.svg` | reduced mathematical body `𝒰` |
| `mobius-ius-relational.svg` | independent `i𝒰s` composition |
| `mobius-bi-seed.svg` | extracted `BI` seed |
| `mobius-bidi-kernel.svg` | portable static BIDI fallback |
| `mobius-bidi-delta.svg` | portable BIDIΔ fallback |

### PNG render directory

```text
assets/identity/renders/
```

| File | Current role |
| --- | --- |
| `mobius-wordmark-static.png` | production-shaped transparent wordmark still |
| `mobius-wordmark-generated.png` | golden frame of generated wordmark |
| `mobius-presence.png` | eye-bearing embodied presence |
| `mobius-ius-phase.png` | isolated `i𝒰s` state |
| `mobius-ui-restored.png` | restored UI state |
| `mobius-hangul-restored.png` | structural `의` state |
| `mobius-bidi-extracted.png` | settled BIDI state |
| `mobius-delta-closed.png` | BIDIΔ closure state |
| `mobius-u-standalone.png` | reduced `𝒰` body state |
| `mobius-u-code.png` | complete canonical `𝒰_` state |
| `mobius-family-lockup.png` | wordmark + BIDIΔ + `𝒰_` family |
| `mobius-one-turn.png` | earlier one-turn supporting render |
| `mobius-restoration.png` | earlier restoration supporting render |
| `mobius-u-operative.png` | earlier operative-body supporting render |
| `mobius-bidi-delta.png` | earlier static BIDIΔ supporting render |

Use the golden and production-shaped files above the “earlier supporting”
renders unless a specific comparison is required.

### MP4 animation directory

```text
assets/identity/motion/
```

| File | Current role |
| --- | --- |
| `mobius-identity-master.mp4` | complete 600-frame, 24-fps working master |
| `mobius-wordmark.mp4` | embodied presence → generated wordmark |
| `mobius-ius.mp4` | relational `i𝒰s` sequence |
| `mobius-ui-hangul.mp4` | `UI → 의` restoration |
| `mobius-bidi-delta.mp4` | `BI → BIDI → Δ` causal extraction and closure |
| `mobius-operator-u.mp4` | reduced operative body `𝒰` |
| `mobius-code-sigil.mp4` | complete canonical Universal Operator `𝒰_`; filename retained for compatibility |

Explicitly ignore this untracked local comparison file:

```text
assets/identity/motion/mobius-identity-master copy.mp4
```

It is not part of the manifest or release package.

### GLB and Blender directory

```text
assets/identity/3d/
```

| File | Current role |
| --- | --- |
| `mobius-identity-master.glb` | full animated identity family |
| `mobius-identity-master.blend` | authoritative editable Blender production scene |
| `mobius-body.glb` | connected embodied Möbius body |
| `mobius-wordmark-animated.glb` | animated wordmark family |
| `mobius-ius-phase-animated.glb` | animated `i𝒰s` |
| `mobius-ius-relational.glb` | static relational composition |
| `mobius-ui-hangul-animated.glb` | animated `UI → 의` |
| `mobius-bidi-delta-animated.glb` | animated BIDIΔ derivation |
| `mobius-bidi-delta.glb` | static BIDIΔ |
| `mobius-u-animated.glb` | animated reduced body `𝒰` |
| `mobius-u-operator.glb` | static reduced body `𝒰` |
| `mobius-u-code-animated.glb` | animated complete `𝒰_` |
| `mobius-u-code-sigil.glb` | static complete `𝒰_` |

## Recommended E.T. Ellis website target

The current Vite site has an empty `public/` directory. Preserve the identity
package's internal relative layout by mounting it here:

```text
/Users/edwardellis/Projects/etellis-website/public/identity/mobius/
```

Recommended result:

```text
public/identity/mobius/
├── 3d/
│   ├── identity-manifest.json
│   └── *.glb
├── motion/
│   └── *.mp4
├── renders/
│   └── *.png
└── *.svg
```

Safe working copy command:

```bash
mkdir -p '/Users/edwardellis/Projects/etellis-website/public/identity/mobius'
rsync -a \
  --exclude '.DS_Store' \
  --exclude '*.blend' \
  --exclude '*.blend1' \
  --exclude 'README.md' \
  --exclude 'mobius-identity-master copy.mp4' \
  '/Users/edwardellis/Documents/Codex/2026-07-20-mobius-identity-system/bidi-coherence-delta-calculus/assets/identity/' \
  '/Users/edwardellis/Projects/etellis-website/public/identity/mobius/'
```

The editable `.blend` scene remains in the identity source repository and must
not be exposed through the website's public directory. Do not add `--delete`
while parallel identity work is still active.

The resulting browser URL base is:

```text
/identity/mobius/
```

Examples:

```text
/identity/mobius/motion/mobius-identity-master.mp4
/identity/mobius/renders/mobius-wordmark-static.png
/identity/mobius/mobius-u-wordmark-dark.svg
/identity/mobius/3d/mobius-identity-master.glb
/identity/mobius/3d/identity-manifest.json
```

## Hot-swap contract

Claude should create one central website adapter, for example:

```text
src/data/mobiusIdentity.ts
```

Recommended base:

```ts
export const MOBIUS_IDENTITY_BASE = "/identity/mobius";
```

Every component should resolve assets through that base or the copied
`identity-manifest.json`. Do not scatter absolute source-machine paths or
duplicate frame ranges through React components.

Preserve these stable consumer roles:

| Consumer role | Working asset |
| --- | --- |
| primary static wordmark | `mobius-u-wordmark-dark.svg` or light variant |
| transparent production still | `renders/mobius-wordmark-static.png` |
| embodied poster/fallback | `renders/mobius-presence.png` |
| full motion | `motion/mobius-identity-master.mp4` |
| wordmark motion | `motion/mobius-wordmark.mp4` |
| Korean relational motion | `motion/mobius-ui-hangul.mp4` |
| kernel motion | `motion/mobius-bidi-delta.mp4` |
| canonical operator sigil motion | `motion/mobius-code-sigil.mp4` |
| interactive spatial master | `3d/mobius-identity-master.glb` |
| reduced-motion semantic states | PNG golden frames |

When a final asset arrives, replace the file at the same consumer role or update
only the central adapter/manifest. Do not restructure the website around the
current curve details.

## Suggested website placement

The live site exposes these relevant implementation surfaces:

```text
src/content/OrgansSection/index.tsx
src/content/TheorySection/index.tsx
src/content/ProductsSection/index.tsx
src/scenes/HubScene/index.tsx
src/components/Wordmark.tsx
```

Recommended use:

| Site surface | Identity use |
| --- | --- |
| global wordmark / compact nav | static dark/light SVG only |
| Hub / Organs poster | `mobius-presence.png`; one restrained transition on focus or consented gaze dwell |
| Möbius organ detail | master MP4 first; GLB only after the lightweight path is stable |
| Theory | explain `𝒰_`, `bidiγΔ`, CDC, one-turn/two-turn, and the evidence hierarchy |
| Products | use the embodied Möbius/product meaning; do not imply the Universal Operator is product-specific |
| deep identity explainer | optional `i𝒰s`, `UI → 의`, and `BI → BIDI → Δ` clips |

Do not front-load every nested meaning on the landing frame. The first read
must remain legible; deeper readings should be discovered through interaction,
motion, or an explicit explainer.

## Accessibility and runtime rules

1. Honor `prefers-reduced-motion`.
2. Use `mobius-wordmark-static.png` or the appropriate SVG as the still.
3. Provide semantic alt text for the immediate visible state, not the entire
   mythology.
4. Never encode a state difference through color alone.
5. Do not activate camera-based gaze without explicit permission.
6. Pointer, touch, keyboard, voice, and consented gaze must reach equivalent
   content states.
7. Load MP4 before GLB unless the interaction genuinely needs spatial geometry.
8. Preload video metadata, not the entire master on the initial page.
9. Preserve literal eyes at sizes where the embodied product is meant to read
   as a character.
10. At favicon scale use the solid monochrome `𝒰_`; do not force a keyline that
    cannot survive rasterization.

## What remains intentionally open

The package is advanced and internally coherent, but these are still working:

- final letter spacing and baseline nudges;
- final `𝒰`/`s` optical coupling;
- final operator keyline thickness;
- final lighting and flat-color balance;
- final eye scale, gaze cadence, blink, and wink behaviors;
- final Korean-restoration timing and optical alignment;
- reverse-sheet `SUI` animation;
- authored `OM` eye-lift reveal;
- full Yi color/state progression;
- final website placement and pacing after in-context review.

Claude may integrate the working versions now, but must keep the asset boundary
replaceable.

## What not to do

- Do not rename `.cdc` to `.u` as part of website integration.
- Do not describe bare `𝒰` as a separate operator from `𝒰_`.
- Do not replace the literal eyes with decorative dots.
- Do not paste `의`, Chinese characters, an eye icon, arrows, or an infinity
  symbol into the static wordmark.
- Do not claim the Yi sequence is an etymology.
- Do not claim reverse `SUI` proves Latin caused the name.
- Do not claim `OM` is already literally spelled by reversal.
- Do not present origin synchronicity as statistical proof of retrocausality.
- Do not use a generic AI gradient or glossy neon treatment to “finish” the
  current assets.
- Do not bypass the identity manifest with duplicated timing constants.
- Do not ingest the untracked comparison MP4.

## Current verification status

At the working baseline above:

- identity release gate passes;
- Blender topology validation passes;
- 10 SVGs, 12 GLBs, 10 golden renders, the MP4 master, and six independent
  clips are packaged;
- the master timeline is 600 frames at 24 fps;
- 238/238 calculus expectations pass;
- 4,811 native witnesses pass;
- Lean and Coq proof checks pass;
- the paper compiles.

This verifies the package and its declared lineage. It does not mean the final
optical design pass is complete.

## Claude handoff summary

Proceed with the website using the current package as a near-final,
manifest-backed identity dependency. Preserve the public hierarchy:

```text
Universal Operator 𝒰_
  ↓ contains open bidiγΔ relation
CDC formalizes one executable kernel
Möbius is the embodied product
Möbi𝒰s is the product-linked language/runtime identity
```

Integrate the immediate meanings first:

```text
embodied ö
→ i / 𝒰 / us
→ one-turn inversion and two-turn restoration
→ BI / reflected DI / BIDI / Δ
```

Reserve `SUI`, `OM`, and the full Yi progression for clearly labeled,
replaceable deep-layer interactions until those animations are finished.
